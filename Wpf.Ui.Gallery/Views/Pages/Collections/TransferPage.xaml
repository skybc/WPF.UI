<Page
    x:Class="Wpf.Ui.Gallery.Views.Pages.Collections.TransferPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Wpf.Ui.Gallery.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Wpf.Ui.Gallery.Views.Pages.Collections"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="TransferPage"
    d:DataContext="{d:DesignInstance local:TransferPage,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="600"
    d:DesignWidth="900"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">

    <StackPanel Margin="0,0,0,24">
        <!-- Basic Transfer Example -->
        <controls:ControlExample
            Margin="0"
            HeaderText="Basic Transfer Control">
            <controls:ControlExample.XamlCode>
                &lt;ui:TransferControl
                \n
                \tLeftItemsSource="{Binding ViewModel.AvailableItems}"
                \n
                \tRightItemsSource="{Binding ViewModel.SelectedItems}"
                \n
                \tTransferMode="Multiple" /&gt;
            </controls:ControlExample.XamlCode>
            <ui:Transfer
                LeftItemsSource="{Binding ViewModel.AvailableItems, Mode=TwoWay}"
                RightItemsSource="{Binding ViewModel.SelectedItems, Mode=TwoWay}"
                TransferMode="Multiple" />
        </controls:ControlExample>

        <!-- Transfer with Custom Height -->
        <controls:ControlExample
            Margin="0,36,0,0"
            HeaderText="Transfer Control with custom heights">
            <controls:ControlExample.XamlCode>
                &lt;ui:TransferControl
                \n
                \tLeftItemsSource="{Binding ViewModel.AvailableItems}"
                \n
                \tRightItemsSource="{Binding ViewModel.SelectedItems}"
                \n
                \tLeftListBoxHeight="200"
                \n
                \tRightListBoxHeight="200" /&gt;
            </controls:ControlExample.XamlCode>
            <ui:Transfer
                LeftItemsSource="{Binding ViewModel.AvailableItems}"
                RightItemsSource="{Binding ViewModel.SelectedItems}"
                LeftListBoxHeight="200"
                RightListBoxHeight="200"
                TransferMode="Multiple" />
        </controls:ControlExample>
    </StackPanel>
</Page>

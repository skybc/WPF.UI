<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Wpf.Ui.Controls"
    xmlns:system="clr-namespace:System;assembly=System.Runtime">

    <Thickness x:Key="TimePickerBorderThemeThickness">1,1,1,1</Thickness>

    <!--  TimeSpan Converters  -->
    <controls:TimeSpanToHourConverter x:Key="TimeSpanToHourConverter" />
    <controls:TimeSpanToMinuteConverter x:Key="TimeSpanToMinuteConverter" />
    <controls:TimeSpanToAMPMConverter x:Key="TimeSpanToAMPMConverter" />

    <Style x:Key="DefaultUiTimePickerStyle" TargetType="{x:Type controls:TimePicker}">
        <!--  Universal WPF UI focus  -->
        <Setter Property="FocusVisualStyle" Value="{DynamicResource DefaultControlFocusVisualStyle}" />
        <!--  Universal WPF UI focus  -->
        <!--  Universal WPF UI ContextMenu  -->
        <Setter Property="ContextMenu" Value="{DynamicResource DefaultControlContextMenu}" />
        <!--  Universal WPF UI ContextMenu  -->
        <Setter Property="Foreground" Value="{DynamicResource TimePickerButtonForegroundDefault}" />
        <Setter Property="Background" Value="{DynamicResource TimePickerButtonBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ControlElevationBorderBrush}" />
        <Setter Property="BorderThickness" Value="{StaticResource TimePickerBorderThemeThickness}" />
        <Setter Property="FontSize" Value="{DynamicResource ControlContentThemeFontSize}" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Top" />
        <Setter Property="MinHeight" Value="{DynamicResource TextControlThemeMinHeight}" />
        <Setter Property="MinWidth" Value="{DynamicResource TextControlThemeMinWidth}" />
        <Setter Property="Padding" Value="{DynamicResource TextControlThemePadding}" />
        <Setter Property="Border.CornerRadius" Value="{DynamicResource ControlCornerRadius}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:TimePicker}">
                    <Grid
                        Width="{TemplateBinding Width}"
                        HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                        VerticalAlignment="{TemplateBinding VerticalAlignment}">
                        <Border
                            x:Name="ContentBorder"
                            MinWidth="{TemplateBinding MinWidth}"
                            MinHeight="{TemplateBinding MinHeight}"
                            Padding="0"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{TemplateBinding Border.CornerRadius}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <!--  Hour Display  -->
                                <TextBlock
                                    Grid.Column="0"
                                    Margin="6,0"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    FontSize="16"
                                    FontWeight="SemiBold"
                                    Foreground="{TemplateBinding Foreground}"
                                    Text="{TemplateBinding Time,
                                                           Converter={StaticResource TimeSpanToHourConverter}}" />

                                <!--  Separator  -->
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="2,0"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    FontSize="16"
                                    Foreground="{TemplateBinding Foreground}"
                                    Text=":" />

                                <!--  Minute Display  -->
                                <TextBlock
                                    Grid.Column="2"
                                    Margin="6,0"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    FontSize="16"
                                    FontWeight="SemiBold"
                                    Foreground="{TemplateBinding Foreground}"
                                    Text="{TemplateBinding Time,
                                                           Converter={StaticResource TimeSpanToMinuteConverter}}" />

                                <!--  AM/PM Separator  -->
                                <TextBlock
                                    Grid.Column="3"
                                    Margin="6,0"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Foreground="{TemplateBinding Foreground}"
                                    Text=" " />

                                <!--  AM/PM Display (only for 12-hour clock)  -->
                                <TextBlock
                                    x:Name="AMPMTextBlock"
                                    Grid.Column="4"
                                    Margin="2,0,6,0"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    FontSize="14"
                                    Foreground="{TemplateBinding Foreground}"
                                    Text="{TemplateBinding Time,
                                                           Converter={StaticResource TimeSpanToAMPMConverter}}"
                                    Visibility="Visible" />
                            </Grid>
                        </Border>

                        <Popup
                            x:Name="TimePickerPopup"
                            AllowsTransparency="True"
                            Placement="Bottom"
                            PlacementTarget="{Binding ElementName=ContentBorder}">
                            <Border
                                Padding="12"
                                Background="{DynamicResource ControlFillColorDefaultBrush}"
                                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="{DynamicResource ControlCornerRadius}">
                                <StackPanel>
                                    <!--  Hour Selector  -->
                                    <StackPanel Margin="0,0,0,12">
                                        <TextBlock
                                            Margin="0,0,0,8"
                                            FontSize="12"
                                            FontWeight="SemiBold"
                                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                            Text="小时" />
                                        <Grid Margin="0,0,0,4">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <Slider
                                                x:Name="HourSlider"
                                                Grid.Column="0"
                                                Height="32"
                                                VerticalAlignment="Center"
                                                IsSnapToTickEnabled="True"
                                                Maximum="23"
                                                Minimum="0"
                                                TickFrequency="1" />
                                            <TextBlock
                                                Grid.Column="1"
                                                MinWidth="40"
                                                Margin="12,0,0,0"
                                                FontSize="14"
                                                FontWeight="SemiBold"
                                                Text="{Binding Value, ElementName=HourSlider, StringFormat='00'}" />
                                        </Grid>
                                    </StackPanel>

                                    <!--  Minute Selector  -->
                                    <StackPanel Margin="0,0,0,12">
                                        <TextBlock
                                            Margin="0,0,0,8"
                                            FontSize="12"
                                            FontWeight="SemiBold"
                                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                            Text="分钟" />
                                        <Grid Margin="0,0,0,4">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <Slider
                                                x:Name="MinuteSlider"
                                                Grid.Column="0"
                                                Height="32"
                                                VerticalAlignment="Center"
                                                IsSnapToTickEnabled="True"
                                                Maximum="59"
                                                Minimum="0"
                                                TickFrequency="1" />
                                            <TextBlock
                                                Grid.Column="1"
                                                MinWidth="40"
                                                Margin="12,0,0,0"
                                                FontSize="14"
                                                FontWeight="SemiBold"
                                                Text="{Binding Value, ElementName=MinuteSlider, StringFormat='00'}" />
                                        </Grid>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource TimePickerButtonBackgroundPointerOver}" />
                            <Setter Property="Foreground" Value="{DynamicResource TimePickerButtonForegroundPointerOver}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background" Value="{DynamicResource TimePickerButtonBackgroundDisabled}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource TimePickerButtonBorderBrushDisabled}" />
                            <Setter Property="Foreground" Value="{DynamicResource TimePickerButtonForegroundDisabled}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{DynamicResource TimePickerButtonBackgroundPressed}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource TimePickerButtonBorderBrushPressed}" />
                            <Setter Property="Foreground" Value="{DynamicResource TimePickerButtonForegroundPressed}" />
                        </Trigger>
                        <Trigger Property="ClockIdentifier" Value="Clock24Hour">
                            <Setter TargetName="AMPMTextBlock" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource DefaultUiTimePickerStyle}" TargetType="{x:Type controls:TimePicker}" />

</ResourceDictionary>